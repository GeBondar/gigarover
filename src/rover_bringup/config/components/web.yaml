web_gateway_node:
  ros__parameters:
    bind_address: 0.0.0.0
    port: 8765
    command_topic: /cmd_vel
    camera_node_name: /usb_camera_node
    vision_node_name: /camera_detector_node
    lidar_node_name: /sllidar_node
    led_strip_node_name: /led_strip_node
    octoliner_node_name: /octoliner_node
    voice_node_name: /waveshare_audio_node
    voice_text_topic: /voice/text
    voice_status_topic: /waveshare_audio/status
    voice_package: rover_waveshare_audio
    voice_executable: waveshare_audio_node
    voice_config_file: ''
    voice_enabled_on_start: false
    drive_command_timeout_sec: 0.25
    # GIGAROVER: лимиты веб-джойстика под ходовую VESC (колесо до 1.6 м/с).
    # Драйвер дополнительно ограничивает скорость колеса и пропорционально
    # масштабирует команду, так что превышение безопасно, но UI честнее
    # держать в пределах реальных возможностей шасси.
    default_linear_speed_mps: 0.5
    default_lateral_speed_mps: 0.16
    default_angular_speed_radps: 1.0
    max_linear_speed_mps: 1.5
    # Скид-стир: боковой скорости нет, linear.y драйвер игнорирует.
    max_lateral_speed_mps: 0.35
    # 3.0 рад/с -> 0.6 м/с на колесо при развороте на месте (track 0.40 м).
    max_angular_speed_radps: 3.0
    plans_directory: ~/.local/share/sverh-rover-web/plans
    hackathon_files_root: ~/sverk_rover/hackathon_files
    maps_root: ~/sverk_rover/src/motion/rover_navigation/maps/current
    terminal_enabled: true
    terminal_url: ''
    terminal_port: 7681
    terminal_path: /
    rosboard_enabled: true
    rosboard_port: 8888
    servo_enabled: true
    odom_topic: /odom
    wheel_odom_topic: /wheel/odometry
    imu_topic: /imu/data
    diagnostics_topic: /diagnostics
    activity_limit: 1000
    stop_hold_sec: 0.75
